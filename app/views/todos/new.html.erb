<%= form_with(model:@todo, local:true) do |f| %>
<%= f.label :description %>
<%= f.text_field :description %>

<%= f.label :completed %>
<%= f.select :completed, options_for_select(['true','false'])%>

<%= f.submit 'Guardar' %>

<% end %> 
<%=link_to "Back", root_path%>